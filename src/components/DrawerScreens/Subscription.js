import React from 'react'
import { View, Text } from 'react-native'

const Subscription = () => {
    return(
        <View style={{ justifyContent:'center',alignItems:'center' }}>
          <Text style={{ fontSize:30,fontWeight:'bold' }}>Subscription Screen</Text>
        </View>
    )
}

export default Subscription;