import React from 'react'
import { View, Text } from 'react-native'

const Help = () => {
    return(
        <View style={{ justifyContent:'center',alignItems:'center' }}>
          <Text style={{ fontSize:30,fontWeight:'bold' }}>Help and Support Screen</Text>
        </View>
    )
}

export default Help;